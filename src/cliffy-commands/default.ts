/**
 * This is the default command function.
 * Will be run when no explicit command is given.
 *
 * It contains an interactive process for detecting and upgrading existing portable vscode installations.
 */

export const dummy = "";
